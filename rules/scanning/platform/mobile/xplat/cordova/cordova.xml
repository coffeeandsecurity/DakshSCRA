<root>
    <category name="Framework-Specific Issues - Cordova">
        <rule>
            <name>Overly Permissive Cordova Whitelist</name>
            <regex>&lt;allow-navigation\s+href="\*"\s*/&gt;|&lt;access\s+origin="\*"\s*/&gt;</regex>
            <rdl_ref>logic/mobile/xplat/cordova/core/overly_permissive_cordova_whitelist.rdl</rdl_ref>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>2</context_lines_before>
                <context_lines_after>1</context_lines_after>
                <aggregate>file</aggregate>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>yellow</highlight_color>
            </scan_config>
            <rule_desc>Detects wildcard navigation/network whitelist entries in Cordova config.</rule_desc>
            <vuln_desc>Wildcard allowlists can expose the app to untrusted content, data leakage, and code abuse.</vuln_desc>
            <developer>Use strict origin allowlists and remove wildcard entries from production config.xml.</developer>
            <reviewer>Verify whitelist rules are limited to approved domains and protocols. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
        <rule>
            <name>Insecure External URL Handling</name>
            <regex>\b(?:window\.open|cordova\.InAppBrowser\.open)\s*\(</regex>
            <rdl_ref>logic/mobile/xplat/cordova/core/insecure_external_url_handling.rdl</rdl_ref>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>2</context_lines_before>
                <context_lines_after>2</context_lines_after>
                <aggregate>none</aggregate>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>magenta</highlight_color>
            </scan_config>
            <rule_desc>Detects browser launch calls that require URL validation and allowlist checks.</rule_desc>
            <vuln_desc>Unvalidated URL launches can enable phishing and malicious redirection.</vuln_desc>
            <developer>Validate target URLs against approved schemes/domains before opening.</developer>
            <reviewer>Check URL construction and ensure strict allowlist validation exists. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
        <rule>
            <name>Sensitive Data Stored in Web Storage</name>
            <regex>\b(?:localStorage\.setItem|sessionStorage\.setItem)\s*\(</regex>
            <rdl_ref>logic/mobile/xplat/cordova/core/sensitive_data_stored_in_web_storage.rdl</rdl_ref>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>2</context_lines_before>
                <context_lines_after>1</context_lines_after>
                <aggregate>none</aggregate>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>yellow</highlight_color>
            </scan_config>
            <rule_desc>Detects web storage writes that may include sensitive values.</rule_desc>
            <vuln_desc>Web storage is not suitable for high-value secrets and may leak data on compromised devices.</vuln_desc>
            <developer>Use secure native storage plugins for credentials and tokens.</developer>
            <reviewer>Verify sensitive data classes are excluded from web storage usage. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
    </category>
    <category name="Framework-Specific Security">
        <rule>
            <name>Cordova Wildcard Access Origin</name>
            <regex>&lt;access\s+origin=\"\*\"</regex>
            <rdl_ref>logic/mobile/xplat/cordova/core/cordova_wildcard_access_origin.rdl</rdl_ref>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>1</context_lines_before>
                <context_lines_after>1</context_lines_after>
                <aggregate>file</aggregate>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>yellow</highlight_color>
            </scan_config>
            <rule_desc>Detects wildcard access origin in Cordova config.</rule_desc>
            <vuln_desc>Wildcard origin allows broad network access and expands attack surface.</vuln_desc>
            <developer>Restrict access origins to explicit trusted domains.</developer>
            <reviewer>Review config.xml access policy entries. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
        <rule>
            <name>Cordova Wildcard Allow Navigation</name>
            <regex>&lt;allow-navigation\s+href=\"\*\"</regex>
            <rdl_ref>logic/mobile/xplat/cordova/core/cordova_wildcard_allow_navigation.rdl</rdl_ref>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>1</context_lines_before>
                <context_lines_after>1</context_lines_after>
                <aggregate>file</aggregate>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>yellow</highlight_color>
            </scan_config>
            <rule_desc>Detects wildcard navigation policy in Cordova config.</rule_desc>
            <vuln_desc>Wildcard navigation permits untrusted content navigation.</vuln_desc>
            <developer>Restrict navigation destinations and enforce CSP.</developer>
            <reviewer>Validate navigation allowlist for least privilege. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
        <rule>
            <name>Cordova Unsafe CSP Directives</name>
            <regex>unsafe-inline|unsafe-eval</regex>
            <rdl_ref>logic/mobile/xplat/cordova/core/cordova_unsafe_csp_directives.rdl</rdl_ref>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>1</context_lines_before>
                <context_lines_after>1</context_lines_after>
                <aggregate>none</aggregate>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>yellow</highlight_color>
            </scan_config>
            <rule_desc>Detects weak CSP directives in Cordova web assets/config.</rule_desc>
            <vuln_desc>Unsafe directives increase XSS/script injection risk.</vuln_desc>
            <developer>Harden CSP and avoid unsafe directives where feasible.</developer>
            <reviewer>Audit CSP config in HTML/meta headers and templates. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
        <rule>
            <name>Cordova Insecure HTTP Endpoint Usage</name>
            <regex>http://</regex>
            <rdl_ref>logic/mobile/xplat/cordova/core/cordova_insecure_http_endpoint_usage.rdl</rdl_ref>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>1</context_lines_before>
                <aggregate>file</aggregate>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>yellow</highlight_color>
            </scan_config>
            <rule_desc>Detects cleartext endpoint references in Cordova code/config.</rule_desc>
            <vuln_desc>Cleartext traffic can expose user and session data.</vuln_desc>
            <developer>Use HTTPS endpoints and strict transport controls.</developer>
            <reviewer>Audit endpoint constants and plugin config values. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
        <rule>
            <name>Cordova WebView JavaScript Bridge Marker</name>
            <regex>addJavascriptInterface\(|cordova\.exec\(</regex>
            <rdl_ref>logic/mobile/xplat/cordova/core/cordova_webview_javascript_bridge_marker.rdl</rdl_ref>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>2</context_lines_before>
                <context_lines_after>2</context_lines_after>
                <aggregate>none</aggregate>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>magenta</highlight_color>
            </scan_config>
            <rule_desc>Detects JavaScript-native bridge usage requiring trust-boundary review.</rule_desc>
            <vuln_desc>Unsafe bridge handling can expose native capabilities to untrusted content.</vuln_desc>
            <developer>Constrain bridge usage and validate inbound arguments.</developer>
            <reviewer>Review plugin bridge interfaces and content trust assumptions. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
        <rule>
            <name>Cordova Hardcoded Secret Marker</name>
            <regex>(?i)(api[_-]?key|secret|token|password)\s*[:=]\s*[\'\"][^\'\"]{8,}[\'\"]</regex>
            <rdl_ref>logic/mobile/xplat/cordova/core/cordova_hardcoded_secret_marker.rdl</rdl_ref>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>1</context_lines_before>
                <aggregate>file</aggregate>
                <report_format>secret_list</report_format>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>red</highlight_color>
            </scan_config>
            <rule_desc>Detects likely hardcoded secrets in Cordova projects.</rule_desc>
            <vuln_desc>Embedded secrets can be recovered from packaged app assets.</vuln_desc>
            <developer>Use backend-managed secrets and rotate exposed values.</developer>
            <reviewer>Validate no static production secrets are shipped. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
    </category>
    <category name="Platform-Specific Issues - Android">
        <rule>
            <name>Android Cleartext Traffic Allowed</name>
            <regex>usesCleartextTraffic\s*=\s*"?(?:true|TRUE)"?</regex>
            <rdl_ref>logic/mobile/xplat/cordova/core/android_cleartext_traffic_allowed.rdl</rdl_ref>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>1</context_lines_before>
                <context_lines_after>1</context_lines_after>
                <aggregate>file</aggregate>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>red</highlight_color>
            </scan_config>
            <rule_desc>Detects insecure cleartext traffic settings in Android-specific Cordova builds.</rule_desc>
            <vuln_desc>Allowing cleartext transport weakens confidentiality and integrity.</vuln_desc>
            <developer>Disable cleartext traffic and use HTTPS for all endpoints.</developer>
            <reviewer>Validate Android network policy enforces secure transport. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
    </category>
    <category name="Platform-Specific Issues - iOS">
        <rule>
            <name>iOS ATS Disabled</name>
            <regex>NSAllowsArbitraryLoads\s*&lt;/key&gt;\s*&lt;true\s*/&gt;</regex>
            <rdl_ref>logic/mobile/xplat/cordova/core/ios_ats_disabled.rdl</rdl_ref>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>2</context_lines_before>
                <context_lines_after>1</context_lines_after>
                <aggregate>file</aggregate>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>red</highlight_color>
            </scan_config>
            <rule_desc>Detects App Transport Security disablement in iOS Cordova builds.</rule_desc>
            <vuln_desc>Disabling ATS increases risk of network interception and tampering.</vuln_desc>
            <developer>Retain ATS defaults and define narrow exceptions only when strictly required.</developer>
            <reviewer>Check Info.plist transport policy and exception domain scope. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
    </category>
</root>
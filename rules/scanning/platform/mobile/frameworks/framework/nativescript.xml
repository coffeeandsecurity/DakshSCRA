<root>
    <category name="Framework-Specific Security">
        <rule>
            <name>NativeScript Debug/Dev Marker</name>
            <regex><![CDATA[NODE_ENV\s*=\s*[\'\"]development[\'\"]|debug\s*[:=]\s*true]]></regex>
            <rdl><![CDATA[[FLAG:NODE_ENV\s*=\s*[\'\"]development[\'\"]|debug\s*[:=]\s*true][IF(MISSING:NODE_ENV\s*=\s*[\'\"]production[\'\"])]]]></rdl>
            <rule_desc>Detects development mode markers in NativeScript projects.</rule_desc>
            <vuln_desc>Debug settings may expose diagnostics and weaken runtime posture.</vuln_desc>
            <developer>Ensure release builds disable debug mode.</developer>
            <reviewer>Review build/runtime environment toggles. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
        <rule>
            <name>NativeScript Insecure HTTP Usage</name>
            <regex>http://</regex>
            <rdl><![CDATA[[FLAG:http://][IF(MISSING:https://)]]]></rdl>
            <rule_desc>Detects cleartext endpoint usage in NativeScript code/config.</rule_desc>
            <vuln_desc>Cleartext traffic can expose sensitive network data.</vuln_desc>
            <developer>Use HTTPS endpoints and secure transport defaults.</developer>
            <reviewer>Audit endpoint declarations and request helpers. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
        <rule>
            <name>NativeScript WebView Script Execution Marker</name>
            <regex><![CDATA[WebView|evaluateJavaScript\(|javaScriptEnabled]]></regex>
            <rdl><![CDATA[[FLAG:WebView|evaluateJavaScript\(|javaScriptEnabled][IF(MISSING:originWhitelist|onShouldOverrideUrlLoading)]]]></rdl>
            <rule_desc>Detects WebView and dynamic script execution patterns.</rule_desc>
            <vuln_desc>Untrusted WebView/script execution can increase injection risk.</vuln_desc>
            <developer>Restrict content origins and harden JS bridge interactions.</developer>
            <reviewer>Review webview bindings and message handlers. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
        <rule>
            <name>NativeScript Local Storage Secret Marker</name>
            <regex><![CDATA[(application-settings|secure-storage|localStorage)\.(setString|setItem)\(.*(token|secret|password|apikey)]]></regex>
            <rdl><![CDATA[[FLAG:(application-settings|secure-storage|localStorage)\.(setString|setItem)\(.*(token|secret|password|apikey)][IF(MISSING:Keychain|Keystore)]]]></rdl>
            <rule_desc>Detects potentially sensitive values written to local storage APIs.</rule_desc>
            <vuln_desc>Improper local persistence can leak credentials/tokens.</vuln_desc>
            <developer>Use secure storage primitives for secrets.</developer>
            <reviewer>Check storage keys and data classification. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
        <rule>
            <name>NativeScript TLS Bypass Marker</name>
            <regex><![CDATA[allowInvalidCertificates|InsecureSkipVerify|trustAll]]></regex>
            <rdl><![CDATA[[FLAG:allowInvalidCertificates|InsecureSkipVerify|trustAll][IF(MISSING:certificatePinning|publicKeyPinning)]]]></rdl>
            <rule_desc>Detects certificate/trust bypass indicators in networking code.</rule_desc>
            <vuln_desc>TLS bypass weakens channel security and enables MITM attacks.</vuln_desc>
            <developer>Enforce strict certificate validation.</developer>
            <reviewer>Audit HTTP client trust configuration. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
        <rule>
            <name>NativeScript Hardcoded Secret Marker</name>
            <regex><![CDATA[(?i)(api[_-]?key|secret|token|password)\s*[:=]\s*[\'\"][^\'\"]{8,}[\'\"]]]></regex>
            <rdl><![CDATA[[FLAG:(api[_-]?key|secret|token|password)\s*[:=]\s*[\'\"][^\'\"]{8,}[\'\"]][IF(MISSING:development)]]]></rdl>
            <rule_desc>Detects likely hardcoded secrets in NativeScript projects.</rule_desc>
            <vuln_desc>Hardcoded credentials can be extracted from app artifacts.</vuln_desc>
            <developer>Avoid embedding secrets and rotate compromised values.</developer>
            <reviewer>Confirm no production secrets exist in source/config. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
    </category>
</root>

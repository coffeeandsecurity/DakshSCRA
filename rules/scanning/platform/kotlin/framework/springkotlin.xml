<root>
    <category name="Framework-Specific Security">
        <rule>
            <name>Springkotlin Debug/Development Build Exposure</name>
            <regex><![CDATA[BuildConfig\.DEBUG|debuggable\s+true]]></regex>
            <rdl><![CDATA[[FLAG:BuildConfig\.DEBUG|debuggable\s+true][IF(MISSING:release|production)]]]></rdl>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>1</context_lines_before>
                <context_lines_after>1</context_lines_after>
                <aggregate>file</aggregate>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>yellow</highlight_color>
            </scan_config>
            <rule_desc>Detects debug/development flags in Kotlin app/service code.</rule_desc>
            <vuln_desc>Debug-enabled builds can increase runtime information exposure.</vuln_desc>
            <developer>Disable debuggable flags in production builds.</developer>
            <reviewer>Validate release build config and runtime debug guards. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
        <rule>
            <name>Springkotlin Insecure Trust Manager / TLS Bypass</name>
            <regex><![CDATA[HostnameVerifier\s*\{\s*.*true\s*\}|TrustAllCerts|setHostnameVerifier\(]]></regex>
            <rdl><![CDATA[[FLAG:HostnameVerifier\s*\{\s*.*true\s*\}|TrustAllCerts|setHostnameVerifier\(]]]></rdl>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>2</context_lines_before>
                <context_lines_after>2</context_lines_after>
                <aggregate>none</aggregate>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>yellow</highlight_color>
            </scan_config>
            <rule_desc>Detects TLS certificate/hostname verification bypass patterns.</rule_desc>
            <vuln_desc>Bypassing TLS validation can enable MITM attacks.</vuln_desc>
            <developer>Use strict certificate and hostname verification.</developer>
            <reviewer>Inspect HTTP client TLS configuration for bypasses. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
        <rule>
            <name>Springkotlin SQL Injection Sink</name>
            <regex><![CDATA[rawQuery\(\s*"[^"]*"\s*\+|execSQL\(\s*"[^"]*"\s*\+]]></regex>
            <rdl><![CDATA[[FLAG:rawQuery\(\s*"[^"]*"\s*\+|execSQL\(\s*"[^"]*"\s*\+]]]></rdl>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>3</context_lines_before>
                <context_lines_after>2</context_lines_after>
                <aggregate>none</aggregate>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>magenta</highlight_color>
            </scan_config>
            <rule_desc>Detects dynamic SQL concatenation patterns.</rule_desc>
            <vuln_desc>Concatenated SQL may lead to injection vulnerabilities.</vuln_desc>
            <developer>Use parameterized queries and prepared statements.</developer>
            <reviewer>Trace untrusted input into SQL construction sites. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
        <rule>
            <name>Springkotlin Permissive CORS/Origin</name>
            <regex><![CDATA[allowedOrigins\s*=\s*arrayOf\(\s*"\*"\s*\)|origin\s*=\s*"\*"]]></regex>
            <rdl><![CDATA[[FLAG:allowedOrigins\s*=\s*arrayOf\(\s*"\*"\s*\)|origin\s*=\s*"\*"]]]></rdl>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>1</context_lines_before>
                <context_lines_after>1</context_lines_after>
                <aggregate>file</aggregate>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>yellow</highlight_color>
            </scan_config>
            <rule_desc>Detects wildcard origin policy settings.</rule_desc>
            <vuln_desc>Wildcard origins may expose sensitive endpoints/data.</vuln_desc>
            <developer>Restrict origins to trusted domains.</developer>
            <reviewer>Review HTTP/security config for least privilege. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
        <rule>
            <name>Springkotlin Hardcoded Secret Material</name>
            <regex><![CDATA[(?i)(secret|apikey|token|password)\s*[:=]\s*"[^"]{8,}"]]></regex>
            <rdl><![CDATA[[FLAG:(?i)(secret|apikey|token|password)\s*[:=]\s*"[^"]{8,}"][IF(MISSING:example|sample|dummy|test|placeholder)]]]></rdl>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>1</context_lines_before>
                <context_lines_after>0</context_lines_after>
                <aggregate>file</aggregate>
                <report_format>secret_list</report_format>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>red</highlight_color>
            </scan_config>
            <rule_desc>Detects likely hardcoded secrets.</rule_desc>
            <vuln_desc>Hardcoded secrets can be extracted from source/apk artifacts.</vuln_desc>
            <developer>Use secure config/secret stores and rotate leaked values.</developer>
            <reviewer>Validate no production secrets are committed. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
    </category>
</root>

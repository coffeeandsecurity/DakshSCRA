<root>
    <category name="Framework-Specific Security">
        <rule>
            <name>Spring Actuator/Management Overexposure</name>
            <regex>management\.endpoints\.web\.exposure\.include\s*=\s*\*|/actuator</regex>
            <rdl_ref>logic/java/framework/spring/spring_actuator_management_overexposure.rdl</rdl_ref>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>1</context_lines_before>
                <context_lines_after>1</context_lines_after>
                <aggregate>file</aggregate>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>yellow</highlight_color>
            </scan_config>
            <rule_desc>Detects potentially overexposed management endpoints.</rule_desc>
            <vuln_desc>Overexposed management endpoints can leak operational and sensitive data.</vuln_desc>
            <developer>Limit exposed endpoints and enforce auth/network restrictions.</developer>
            <reviewer>Verify actuator/management endpoints are hardened in production. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
        <rule>
            <name>Spring SQL Query Concatenation Sink</name>
            <regex>createQuery\(\s*"[^"]*"\s*\+|createNativeQuery\(\s*"[^"]*"\s*\+|Statement\s+\w+\s*=\s*.*createStatement\(</regex>
            <rdl_ref>logic/java/framework/spring/spring_sql_query_concatenation_sink.rdl</rdl_ref>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>3</context_lines_before>
                <context_lines_after>2</context_lines_after>
                <aggregate>none</aggregate>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>magenta</highlight_color>
            </scan_config>
            <rule_desc>Detects dynamic SQL/HQL query concatenation patterns.</rule_desc>
            <vuln_desc>Concatenated queries may be vulnerable to SQL injection.</vuln_desc>
            <developer>Use parameterized queries/prepared statements.</developer>
            <reviewer>Inspect tainted data flow into query construction APIs. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
        <rule>
            <name>Spring Permissive CORS</name>
            <regex>@CrossOrigin\(.*\*|allowedOrigins\s*=\s*\{?\s*"\*"</regex>
            <rdl_ref>logic/java/framework/spring/spring_permissive_cors.rdl</rdl_ref>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>1</context_lines_before>
                <context_lines_after>1</context_lines_after>
                <aggregate>file</aggregate>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>yellow</highlight_color>
            </scan_config>
            <rule_desc>Detects wildcard CORS in controller/global config.</rule_desc>
            <vuln_desc>Wildcard CORS can expose endpoints to untrusted web origins.</vuln_desc>
            <developer>Use explicit trusted origin allow-lists.</developer>
            <reviewer>Validate CORS settings across all controllers and profiles. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
        <rule>
            <name>Spring Insecure Transport Setting</name>
            <regex>http://|server\.ssl\.enabled\s*=\s*false|allowInsecureProtocol\s*=\s*true</regex>
            <rdl_ref>logic/java/framework/spring/spring_insecure_transport_setting.rdl</rdl_ref>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>1</context_lines_before>
                <context_lines_after>1</context_lines_after>
                <aggregate>file</aggregate>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>yellow</highlight_color>
            </scan_config>
            <rule_desc>Detects insecure transport usage/settings in code or config.</rule_desc>
            <vuln_desc>Insecure transport can expose credentials and session data.</vuln_desc>
            <developer>Enforce TLS and secure transport defaults.</developer>
            <reviewer>Review transport settings for production hardening. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
        <rule>
            <name>Spring Hardcoded Secret Material</name>
            <regex>(secret|apikey|token|password)\s*[=:]\s*"[^\"]{8,}"</regex>
            <rdl_ref>logic/java/framework/spring/spring_hardcoded_secret_material.rdl</rdl_ref>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>1</context_lines_before>
                <context_lines_after>0</context_lines_after>
                <aggregate>file</aggregate>
                <report_format>secret_list</report_format>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>red</highlight_color>
            </scan_config>
            <rule_desc>Detects likely hardcoded secret values.</rule_desc>
            <vuln_desc>Hardcoded credentials can be extracted and abused.</vuln_desc>
            <developer>Use secret managers or runtime environment variables.</developer>
            <reviewer>Verify credentials are not stored in source/config. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
    </category>
</root>
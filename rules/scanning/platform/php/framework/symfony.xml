<root>
    <category name="Framework-Specific Security">
        <rule>
            <name>Symfony Debug Mode Enabled</name>
            <regex>APP_DEBUG\s*=\s*true|kernel\.debug\s*=\s*true|WP_DEBUG\s*,\s*true|ENVIRONMENT\s*=\s*[\'\"]development[\'\"]</regex>
            <rdl_ref>logic/php/framework/symfony/symfony_debug_mode_enabled.rdl</rdl_ref>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>1</context_lines_before>
                <context_lines_after>1</context_lines_after>
                <aggregate>file</aggregate>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>yellow</highlight_color>
            </scan_config>
            <rule_desc>Detects framework/application debug mode enabled in deployable configuration.</rule_desc>
            <vuln_desc>Debug mode can expose stack traces, internal paths, and sensitive runtime details.</vuln_desc>
            <developer>Disable debug mode in production and separate dev/prod config.</developer>
            <reviewer>Verify production config and runtime env disable debug flags. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
        <rule>
            <name>Symfony Dangerous Dynamic Eval/Template Execution</name>
            <regex>\beval\s*\(|assert\s*\(\s*\$|create_function\s*\(</regex>
            <rdl_ref>logic/php/framework/symfony/symfony_dangerous_dynamic_eval_template_execution.rdl</rdl_ref>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>3</context_lines_before>
                <context_lines_after>2</context_lines_after>
                <aggregate>none</aggregate>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>red</highlight_color>
            </scan_config>
            <rule_desc>Detects dangerous dynamic code execution primitives in PHP code paths.</rule_desc>
            <vuln_desc>Dynamic execution APIs can lead to remote code execution when user input is involved.</vuln_desc>
            <developer>Avoid dynamic execution; use strict allow-lists and safer alternatives.</developer>
            <reviewer>Review data flow into dynamic execution sinks for user-controlled input. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
        <rule>
            <name>Symfony Raw SQL Query Construction</name>
            <regex>DB::raw\(|selectRaw\(|whereRaw\(|-&gt;query\(\s*\$|mysqli_query\(|PDO::query\(</regex>
            <rdl_ref>logic/php/framework/symfony/symfony_raw_sql_query_construction.rdl</rdl_ref>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>3</context_lines_before>
                <context_lines_after>2</context_lines_after>
                <aggregate>none</aggregate>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>red</highlight_color>
            </scan_config>
            <rule_desc>Detects raw/dynamic SQL construction in framework and DB access calls.</rule_desc>
            <vuln_desc>Dynamic SQL with untrusted input can introduce SQL injection vulnerabilities.</vuln_desc>
            <developer>Use parameterized queries/prepared statements and validate input.</developer>
            <reviewer>Check whether untrusted data reaches raw SQL APIs without binding. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
        <rule>
            <name>Symfony Permissive CORS Configuration</name>
            <regex>Access-Control-Allow-Origin\s*[:=]\s*[\'\"]\*[\'\"]|allowed_origins\s*=\s*\[?\s*[\'\"]\*[\'\"]</regex>
            <rdl_ref>logic/php/framework/symfony/symfony_permissive_cors_configuration.rdl</rdl_ref>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>1</context_lines_before>
                <context_lines_after>1</context_lines_after>
                <aggregate>none</aggregate>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>yellow</highlight_color>
            </scan_config>
            <rule_desc>Detects wildcard CORS settings in application or framework config.</rule_desc>
            <vuln_desc>Wildcard CORS may allow untrusted origins to access sensitive endpoints.</vuln_desc>
            <developer>Restrict CORS origins to explicit trusted domains.</developer>
            <reviewer>Validate CORS policies are environment-specific and least-privilege. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
        <rule>
            <name>Symfony Hardcoded Secret Material</name>
            <regex>(APP_KEY|SECRET|API[_-]?KEY|TOKEN|PASSWORD)\s*[:=]\s*[\'\"][^\'\"]{8,}[\'\"]</regex>
            <rdl_ref>logic/php/framework/symfony/symfony_hardcoded_secret_material.rdl</rdl_ref>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>1</context_lines_before>
                <aggregate>file</aggregate>
                <report_format>secret_list</report_format>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>red</highlight_color>
            </scan_config>
            <rule_desc>Detects likely hardcoded secret material in source/config.</rule_desc>
            <vuln_desc>Hardcoded secrets risk leakage through source exposure and artifact distribution.</vuln_desc>
            <developer>Move secrets to secure secret management and rotate compromised values.</developer>
            <reviewer>Review whether detected literals are real credentials and enforce secret hygiene. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
    </category>
</root>
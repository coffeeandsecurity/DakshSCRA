<root>
    <category name="Framework-Specific Issues - Yii Framework">
        <rule>
            <name>ActiveRecord Query with Raw Condition String</name>
            <regex><![CDATA[->where\s*\(\s*["'][^"']*["']\s*\.\s*\$|->where\s*\(\s*["'][^"']*\$\{?|->andWhere\s*\(\s*["'][^"']*["']\s*\.\s*\$|->orWhere\s*\(\s*["'][^"']*["']\s*\.\s*\$]]></regex>
            <rdl><![CDATA[[FLAG:->where\s*\(\s*["'][^"']*["']\s*\.\s*\$|->andWhere\s*\(\s*["'][^"']*["']\s*\.\s*\$|->orWhere\s*\(\s*["'][^"']*["']\s*\.\s*\$|->where\s*\(\s*["'][^"']*\$\{?|->andWhere\s*\(\s*["'][^"']*\$\{?]]]></rdl>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>3</context_lines_before>
                <context_lines_after>2</context_lines_after>
                <aggregate>none</aggregate>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>red</highlight_color>
            </scan_config>
            <rule_desc>Detects Yii ActiveQuery where() conditions built with string concatenation of variables.</rule_desc>
            <vuln_desc>Concatenating variables into where() conditions bypasses Yii's query parameterization, introducing SQL injection vulnerabilities equivalent to raw PDO string concatenation.</vuln_desc>
            <developer>Use Yii's array condition format: ->where(['column' => $value]) or parameterized strings: ->where('column = :val', [':val' => $value]).</developer>
            <reviewer>Confirm all where() conditions use array syntax or named parameter bindings without string concatenation. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
        <rule>
            <name>Unsafe Mass Assignment via load() or setAttributes()</name>
            <regex><![CDATA[->load\s*\(\s*\$_(?:POST|GET|REQUEST)|->setAttributes\s*\(\s*\$_(?:POST|GET|REQUEST)]]></regex>
            <rdl><![CDATA[[FLAG:->load\s*\(\s*\$_(?:POST|GET|REQUEST)|->setAttributes\s*\(\s*\$_(?:POST|GET|REQUEST))[IF(MISSING:safeAttributes|rules|scenarios|safe\s*=>)]]]></rdl>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>3</context_lines_before>
                <context_lines_after>2</context_lines_after>
                <aggregate>none</aggregate>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>red</highlight_color>
            </scan_config>
            <rule_desc>Detects Yii model load() or setAttributes() calls directly using superglobal input arrays.</rule_desc>
            <vuln_desc>Loading raw request data into models without defining safe attributes in validation rules can enable mass assignment attacks, allowing attackers to set privileged attributes (is_admin, role) not intended for user modification.</vuln_desc>
            <developer>Define validation rules() with explicit safe attributes. Specify model scenarios to limit which attributes are mass-assignable per operation (e.g., signup vs update scenarios).</developer>
            <reviewer>Verify model rules() define appropriate safe attributes and that sensitive fields are excluded from mass assignment. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
        <rule>
            <name>createCommand with Raw SQL String</name>
            <regex><![CDATA[->createCommand\s*\(\s*["'][^"']*["']\s*\.\s*\$|->createCommand\s*\(\s*["'][^"']*\$\{?]]></regex>
            <rdl><![CDATA[[FLAG:->createCommand\s*\(\s*["'][^"']*["']\s*\.\s*\$|->createCommand\s*\(\s*["'][^"']*\$\{?]]]></rdl>
            <scan_config>
                <match_mode>line</match_mode>
                <context_lines_before>3</context_lines_before>
                <context_lines_after>2</context_lines_after>
                <aggregate>none</aggregate>
                <highlight_enabled>true</highlight_enabled>
                <highlight_color>red</highlight_color>
            </scan_config>
            <rule_desc>Detects Yii createCommand() calls with SQL built via string concatenation.</rule_desc>
            <vuln_desc>Constructing raw SQL with string concatenation in createCommand() bypasses parameter binding and enables SQL injection.</vuln_desc>
            <developer>Use createCommand() with parameter bindings: $db->createCommand('SELECT * FROM t WHERE id = :id', [':id' => $id])->queryAll().</developer>
            <reviewer>Confirm all createCommand() calls use parameterized queries. This can be confirmed as an issue when the matched path is reachable and required controls are missing, weak, or inconsistent.</reviewer>
        </rule>
    </category>
</root>
